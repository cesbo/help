{{- $.Scratch.Add "index" slice }}
{{- range where .Site.Pages "Section" "docs" }}
    {{- if .IsPage }}
        {{- $.Scratch.Add "index" (dict "title" .Title "contents" (htmlUnescape .Plain) "permalink" .RelPermalink) }}
    {{- end }}
{{- end }}
{{- $.Scratch.Get "index" | jsonify }}
